apiVersion: v1
kind: Service
metadata:
  name: fixbot-service
  namespace: fixbot
  labels:
    app: fixbot
spec:
  type: ClusterIP
  selector:
    app: fixbot
  ports:
    - name: http
      port: 80
      targetPort: 7007
      protocol: TCP
